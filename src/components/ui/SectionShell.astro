---
interface Props {
  id?: string;
  className?: string;
}

const { id, className = "" } = Astro.props;
---

<section id={id} class={`section-shell ${className}`}>
  <div class="container-shell">
    <slot />
  </div>
</section>
